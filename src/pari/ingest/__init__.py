"""Corpus ingestion.

One module per source. Each returns records plus a counted, categorized
accounting of everything that did not make it — `docs/01`'s posture, applied to
questions rather than evidence docs: a corpus that quietly loses part of its
input is not one anyone can reason about.
"""
